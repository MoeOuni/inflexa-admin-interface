import { CustomersTable } from "../data-tables/customers-table";

const CustomersList = () => {
  return (
    <div>
      <CustomersTable data={[]} />
    </div>
  );
};

export default CustomersList;
